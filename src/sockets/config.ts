export enum EVENT {
  DISCONNECTED = 'disconnect',
  CONNECTED = 'connect',
  CONNECTION_FAIL = 'connect_error',
  ERROR = 'error',
  EXCEPTION = 'exception',
}

export enum EXAMPLE_EVENT {
  PAIR_FILTER = 'pairs-filter',
}
